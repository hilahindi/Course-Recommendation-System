# Ignore Python virtual environments
.venv/
venv/
server/.venv/
server/venv/
__pycache__/
*.pyc

# Ignore React dependencies
client/node_modules/
client/dist/

# Ignore local environment variables and database files
.env
server/*.db
server/tests/*.db

# Test / cache artifacts
.pytest_cache/

# Course source files (local only — not committed)
data/
server/data/

# One-time migration scripts (already ran, not needed in repo)
server/src/add_missing_courses.py
server/src/fix_duplicate_track.py
